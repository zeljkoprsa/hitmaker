# Production environment settings for Hitmaker (CRA app)
# These variables are loaded when NODE_ENV=production

# Public environment variables (exposed to browser)
# All REACT_APP_* variables are public and bundled with the app
REACT_APP_APP_NAME=Hitmaker
REACT_APP_USE_NEW_METRONOME=true
REACT_APP_ENVIRONMENT=production
REACT_APP_LOG_LEVEL=error
REACT_APP_ENABLE_PERFORMANCE_MONITORING=true
REACT_APP_BUNDLE_ANALYZER=false

# Analytics & Monitoring (Public)
# These should be configured in your deployment platform with production values
REACT_APP_POSTHOG_KEY=your_production_posthog_key_here
REACT_APP_POSTHOG_HOST=https://eu.i.posthog.com

# Build Configuration (Private - not bundled)
PUBLIC_URL=/hitmaker
GENERATE_SOURCEMAP=false

# NOTE: Private environment variables should be configured in your deployment platform
# Do NOT commit actual secrets to version control
# Examples of what should be configured in deployment:
# - API_SECRET_KEY
# - WEBHOOK_SECRET
# - EXTERNAL_API_KEY
# - DATABASE_URL (if needed)
# - EMAIL_SERVICE_API_KEY (if needed)
